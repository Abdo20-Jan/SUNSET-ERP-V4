// Stub vacío para neutralizar `import "server-only"` bajo Vitest.
// El paquete real lanza un error en tiempo de import fuera del runtime
// server de Next; en los tests no hay tal frontera, así que no hace nada.
export {};
