version: 2

updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "08:00"
      timezone: "America/Argentina/Buenos_Aires"
    open-pull-requests-limit: 10
    versioning-strategy: increase
    labels:
      - dependencies
      - npm
    commit-message:
      prefix: "chore(deps)"
      include: scope
    # Grouping policy:
    # - Patch + minor bundled por área → menos PRs ruidosos
    # - Major isolado em PR individual → revisão dedicada (evita bundles tipo #87)
    groups:
      next-react:
        update-types:
          - minor
          - patch
        patterns:
          - "next"
          - "next-*"
          - "@next/*"
          - "react"
          - "react-*"
          - "@types/react"
          - "@types/react-dom"
      prisma:
        update-types:
          - minor
          - patch
        patterns:
          - "@prisma/*"
          - "prisma"
      tooling:
        update-types:
          - minor
          - patch
        patterns:
          - "eslint"
          - "eslint-*"
          - "@eslint/*"
          - "@biomejs/*"
          - "typescript"
          - "@types/node"
      auth:
        update-types:
          - minor
          - patch
        patterns:
          - "next-auth"
          - "@auth/*"
          - "bcryptjs"
      ui:
        update-types:
          - minor
          - patch
        patterns:
          - "@base-ui/*"
          - "@hugeicons/*"
          - "@tanstack/*"
          - "tailwindcss"
          - "@tailwindcss/*"
          - "tailwind-merge"
          - "tw-animate-css"
          - "class-variance-authority"
          - "clsx"
          - "cmdk"
          - "sonner"
          - "shadcn"

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "08:00"
      timezone: "America/Argentina/Buenos_Aires"
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - github-actions
    commit-message:
      prefix: "chore(actions)"
      include: scope
